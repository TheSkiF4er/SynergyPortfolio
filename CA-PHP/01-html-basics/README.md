# 01 — HTML/CSS

Выполнена семантическая HTML-страница: внешний `style.css`, `h1.greeting`, второй заголовок, абзац, ненумерованный список из 6 пунктов и таблица 5×5 с `thead`, `tbody`, `tfoot` и объединённой итоговой строкой.

Открыть `index.html` в браузере.
