# 13 — Валидация

Все поля обязательны. Имя допускает кириллицу/латиницу/пробел/дефис/апостроф, логин — 2–20 символов `[A-Za-z0-9_-]`, email проверяется `filter_var`, пароль требует оба регистра, цифру, спецсимвол и минимум 6 символов.
