# Zombie process

Дочерний процесс немедленно завершается, родитель намеренно ждёт 3 секунды перед `waitpid()`. В этот интервал `ps -o pid,ppid,state,cmd -p <PID>` показывает состояние `Z`. Затем процесс корректно reap-ится, чтобы пример не оставлял мусор.
