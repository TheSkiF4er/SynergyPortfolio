import {useEffect,useState} from 'react';
function Counter(){const[n,setN]=useState(0);useEffect(()=>{const onKey=e=>{if(e.key==='ArrowUp')setN(x=>x+1);if(e.key==='ArrowDown')setN(x=>x-1)};addEventListener('keydown',onKey);return()=>removeEventListener('keydown',onKey)},[]);return <section className="card"><h2>Counter: {n}</h2><button onClick={()=>setN(x=>x+1)}>+1</button> <button onClick={()=>setN(x=>x-1)}>-1</button><p className="muted">ArrowUp / ArrowDown тоже меняют состояние.</p></section>}
function Timer(){const[seconds,setSeconds]=useState(0);const limit=10;useEffect(()=>{if(seconds>=limit)return;const id=setInterval(()=>setSeconds(x=>x+1),1000);return()=>clearInterval(id)},[seconds]);return <section className="card"><h2>Timer: {seconds}s</h2>{seconds>=limit?<p>Время истекло.</p>:<p>Осталось {limit-seconds} сек.</p>}<button onClick={()=>setSeconds(0)}>Сбросить</button></section>}
export default function App(){return <main><h1>useState / useEffect</h1><Counter/><Timer/></main>}
