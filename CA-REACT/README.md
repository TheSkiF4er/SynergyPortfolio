# CA-REACT — 9 домашних заданий React

Каждая папка — самостоятельное Vite-приложение. Общий шаблон команд:

```bash
cd 03-components
npm install
npm run dev
```

Для задания 07 ключ Guardian передаётся через `.env.local` (`VITE_GUARDIAN_API_KEY`), файл с секретом не коммитится.
