# Тема 10 — RIPv2

Физическая топология темы 3 + loopback `x.x.x.x/24`, где x — номер маршрутизатора. Включён RIPv2, `no auto-summary`; loopback анонсируются через `redistribute connected`. Проверить `show ip route rip` и ping loopback остальных роутеров.
