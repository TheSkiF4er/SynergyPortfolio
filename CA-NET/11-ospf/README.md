# Тема 11 — OSPF

Продолжение темы 10: RIP удаляется, запускается OSPF process 1 area 0; connected loopback redistributing as external E2. Проверки: `show ip route ospf`, `show ip ospf neighbor`, ping физических и loopback адресов.
