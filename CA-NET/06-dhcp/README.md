# Тема 6 — DHCP

R1 — DHCP-server `192.168.1.1/24`; R2–R4 — клиенты. Исключены `.2` и `.3`, DNS выдаются `192.168.1.2` и `.3`, gateway `.1`, lease 15 минут. Проверить `show ip dhcp binding` и `show ip interface brief`.
