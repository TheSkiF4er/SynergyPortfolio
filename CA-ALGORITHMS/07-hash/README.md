# 07 — Hash

`HashMap<String,List<Integer>>` хранит студентов и оценки; `addGrade` отклоняет значения вне диапазона `0…100`. Реализованы частоты целых чисел и среднее значений, читаемых из `Scanner` до `0`. Пример методички `20, 30, 0 → 2.5` арифметически ошибочен; корректный результат — `25.0`.
