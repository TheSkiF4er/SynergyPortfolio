# 03 — Связные списки и selection sort

`findMiddle()` возвращает `Node` и использует slow/fast pointers (`O(n)`, `O(1)` memory); `printReverse()` рекурсивно печатает список наоборот; `selectionSortByAsc(Student[])` сортирует по `point` по возрастанию и возвращает `LinkedList<Student>` за `O(n²)`.
