# 04 — Рекурсия и стек

Рекурсивная сумма, рекурсивный диапазон и проверка скобок через `Deque` как стек (LIFO). В критериях методички встречается слово «очередь», но корректная структура для вложенных скобок — стек.
